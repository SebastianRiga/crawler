{
  "name": "crawler",
  "version": "0.0.0001",
  "description": "A simple dungeon crawler written in typescript using phaser and parcel.",
  "repository": "https://sebastianrigadevelopment@dev.azure.com/sebastianrigadevelopment/Crawler/_git/Crawler",
  "homepage": "./",
  "main": "build/app.js",
  "bugs": {
    "email": "sebastian.riga.development@gmail.com",
    "url": "https://dev.azure.com/sebastianrigadevelopment/Crawler/_workitems/recentlyupdated/"
  },
  "author": "Sebastian Riga",
  "license": "MIT",
  "private": true,
  "keywords": [
    "phaser",
    "parcel",
    "electron",
    "typescript"
  ],
  "scripts": {
    "postinstall": "electron-builder install-app-deps",
    "init": "yarn install",
    "develope:browser": "parcel src/index.html -d build -p 3000",
    "develope:electron": "cross-env ELECTRON_DEBUG=vscode electron --remote-debugging-port=3000 build/app.js",
    "build": "parcel build src/electron.ts src/index.html --public-url ./ -d build --out-file=app --target=electron",
    "dist": "yarn build && electron-builder",
    "clean:dist": "rm -rf dist",
    "clean:build": "rm -rf build",
    "clean:cache": "rm -rf .parcel-cache .cache",
    "clean:node_modules": "rm -rf node_modules",
    "superclean": "yarn clean:dist && yarn clean:build && yarn clean:cache",
    "deinit": "yarn superclean && yarn clean:node_modules"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.9.1",
    "@typescript-eslint/parser": "^4.9.1",
    "cross-env": "^7.0.3",
    "electron": "^11.1.0",
    "electron-builder": "^22.9.1",
    "electron-reload": "^1.5.0",
    "eslint": "^7.15.0",
    "parcel": "^1.12.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "phaser": "^3.24.1"
  },
  "parcelCleanPaths": [
    "dist"
  ],
  "build": {
    "appId": "com.riga.sebastian.crawler",
    "mac": {
      "category": "public.app-category.games"
    },
    "files": [
      "build/*"
    ]
  },
  "staticFiles": {
    "staticPath": "assets",
    "watcherGlob": "**"
  }
}
